import Events from "@/components/events";

export default function Calendar() {
    return (
      <div
        id="calendar"
        className="page"
        style={{ backgroundColor: "#FFF9F0" }}
      >
        <Events />
      </div>
    );
}